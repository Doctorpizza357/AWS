# Backend Server

This is the local Express backend for the Pathfindr app.

## Endpoints

- `GET /health` - simple health check
- `POST /api/assistant/message` - placeholder assistant endpoint for later AI wiring

## Run

```bash
npm install
cd server
npm install
npm start
```
